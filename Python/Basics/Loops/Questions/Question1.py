# Write a program to print multiplication table of a number using for loop (number is assigned by the user)

number = int(input("Enter the number: "))

for i in  range(11): # 11 because the stop value is not recognised
    print(number * i)

